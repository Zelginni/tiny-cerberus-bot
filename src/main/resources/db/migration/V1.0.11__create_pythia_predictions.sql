create table cerberus.pythia_predictions (
    id bigint generated by default as identity primary key,
    prediction_text text not null
);

alter table cerberus.chat add column pythia_enabled boolean;

update cerberus.chat set pythia_enabled = false;
